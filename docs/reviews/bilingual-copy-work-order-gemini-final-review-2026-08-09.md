```json
{
  "decision": "pass",
  "summary": "The bilingual copy work order mechanism and applied P0/P1 updates satisfy all review contract criteria. All 123 tests pass, design contract audits and SVG exports are synchronized, guarded dry-run and rollback protections are verified, and headless Chromium QA confirms layout integrity across both locales and viewports.",
  "findings": [],
  "followUps": [
    "Unify `site.heroRoles` and the static JSON-LD `jobTitle` array in `scripts/build-site.mjs` with the updated role taxonomy upon producer approval.",
    "Apply remaining non-priority workbook rows in a future copy work order when prioritized by editorial."
  ]
}
```